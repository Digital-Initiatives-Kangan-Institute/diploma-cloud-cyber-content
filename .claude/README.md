# `.claude/` — infrastructure skeleton

Standard scaffold present in every repo for consistency. Most folders are empty
placeholders until needed (this repo's `skills/` is actively used). For a full
explanation of what each folder/file is for, see the **umbrella repo's
`.claude/README.md`** (the single teaching reference).
